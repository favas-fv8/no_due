# Django / backend environment configuration
# Copy this file to `.env` and fill in real values. NEVER commit `.env`.
SECRET_KEY=change-me-generate-with-django-get_random_secret_key
DEBUG=True
ALLOWED_HOSTS=*
CORS_ALLOW_ALL_ORIGINS=True
